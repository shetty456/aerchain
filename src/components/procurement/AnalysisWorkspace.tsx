'use client';

import { useEffect, useState } from 'react';
import { ArrowUp, Download, LoaderCircle } from 'lucide-react';

const suggestions = ['Which vendors passed qualification?', 'Which vendor is cheapest overall?', 'Show me the cheapest qualified vendor for each line item.', 'What items are still unresolved?', 'How much do we save with a split award compared with giving everything to the cheapest qualified single vendor?'];
const money = new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 });

export default function AnalysisWorkspace() {
  const [question, setQuestion] = useState('');
  const [answer, setAnswer] = useState<{ question: string; result: Record<string, unknown> } | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);
  async function ask(value = question) {
    if (!value.trim() || loading) return;
    setQuestion(value); setLoading(true); setError(null);
    try {
      const response = await fetch('/api/demo/analysis', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ question: value }) });
      const payload = await response.json();
      if (!response.ok) throw new Error(payload.error || 'Analysis failed.');
      setAnswer(payload);
    } catch (reason) { setError(reason instanceof Error ? reason.message : 'Analysis failed.'); }
    finally { setLoading(false); }
  }
  return <div className="mx-auto max-w-5xl"><div><p className="eyebrow">Procurement analyst</p><h1 className="mt-2 text-2xl font-semibold">Ask about this sourcing event</h1><p className="mt-2 text-sm text-[var(--muted)]">AI interprets the question. Qualification rules and arithmetic run deterministically over normalized data.</p></div><div className="mt-6 flex flex-wrap gap-2">{suggestions.map((item) => <button key={item} onClick={() => void ask(item)} className="rounded-full border border-[var(--line)] bg-white px-3 py-2 text-[11px] hover:border-[var(--line-strong)]">{item}</button>)}</div><div className="mt-5 flex rounded-xl border border-[var(--line-strong)] bg-white p-2"><input value={question} onChange={(event) => setQuestion(event.target.value)} onKeyDown={(event) => { if (event.key === 'Enter') void ask(); }} placeholder="Ask about qualification, pricing, unresolved items, or award scenarios…" className="min-w-0 flex-1 bg-transparent px-3 text-sm outline-none" /><button onClick={() => void ask()} disabled={loading} className="flex size-10 items-center justify-center rounded-lg bg-[var(--ink)] text-white disabled:opacity-50">{loading ? <LoaderCircle className="animate-spin" size={15} /> : <ArrowUp size={15} />}</button></div>{error && <p className="mt-3 text-xs text-[var(--red)]">{error}</p>}{answer && <ResultCard answer={answer} />}<AwardPanel /></div>;
}

function ResultCard({ answer }: { answer: { question: string; result: Record<string, unknown> } }) {
  const result = answer.result;
  const operation = String(result.operation);
  return <section className="mt-6 rounded-xl border border-[var(--line)] bg-white p-5"><p className="text-xs font-semibold">{answer.question}</p><div className="mt-4 text-sm leading-6 text-[var(--muted)]">{operation === 'QUALIFIED_VENDORS' && <p><strong className="text-[var(--ink)]">Qualified:</strong> {((result.qualified as string[]) ?? []).join(', ') || 'None'}</p>}{operation === 'CHEAPEST_OVERALL' && <p>{result.cheapest ? <><strong className="text-[var(--ink)]">{(result.cheapest as { vendor: string }).vendor}</strong> is the cheapest qualified vendor covering all lines at {money.format((result.cheapest as { total: number }).total)}.</> : String(result.caveat)}</p>}{operation === 'UNRESOLVED_ITEMS' && <p><strong className="text-[var(--ink)]">{String(result.count)}</strong> vendor-line responses remain unresolved or ineligible.</p>}{operation === 'SPLIT_AWARD_SAVINGS' && <p>{result.savings !== null ? <>A split award totals <strong className="text-[var(--ink)]">{money.format(Number(result.splitTotal))}</strong> and saves <strong className="text-[var(--green)]">{money.format(Number(result.savings))}</strong> versus the cheapest qualified complete vendor.</> : String(result.caveat)}</p>}{operation === 'CHEAPEST_QUALIFIED_PER_LINE' && <><p>The eligible split award totals <strong className="text-[var(--ink)]">{money.format(Number(result.total))}</strong>. {String(result.unawardedLines)} lines remain unawarded.</p><ResultTable selections={result.selections as Array<{ lineId: string; item: string; vendor: string; unitPrice: number } | null>} /></>}{operation === 'SUMMARY' && <p>{String(result.qualified)} qualified vendors; {String(result.unresolved)} unresolved vendor-line responses.</p>}</div></section>;
}

function ResultTable({ selections }: { selections: Array<{ lineId: string; item: string; vendor: string; unitPrice: number } | null> }) { const rows = selections.filter(Boolean).slice(0, 5); return <div className="mt-4 rounded-lg border border-[var(--line)]"><table className="w-full text-left text-[11px]"><thead className="bg-[var(--surface)]"><tr><th className="px-3 py-2">Line</th><th className="px-3 py-2">Selected vendor</th><th className="px-3 py-2 text-right">Unit price</th></tr></thead><tbody>{rows.map((item) => item && <tr key={item.lineId} className="border-t border-[var(--line)]"><td className="px-3 py-2">{item.lineId} · {item.item}</td><td className="px-3 py-2">{item.vendor}</td><td className="px-3 py-2 text-right">{money.format(item.unitPrice)}</td></tr>)}</tbody></table><p className="border-t border-[var(--line)] px-3 py-2 text-[10px] text-[var(--muted)]">Showing top {rows.length} lines. The full allocation is retained for the award recommendation.</p></div>; }

type Award = { status: 'DRAFT' | 'ACCEPTED'; totalSpend: number; savings?: number; unawardedLines: number; unawardedLineIds?: string[]; spendByVendor: Array<{ vendor: string; spend: number; lines: number }> };
function AwardPanel() { const [award, setAward] = useState<Award | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null); useEffect(() => { fetch('/api/demo/award').then((response) => response.json()).then((payload) => setAward(payload.award)).catch(() => undefined); }, []); async function mutate(method: 'POST' | 'PUT') { setBusy(true); setError(null); try { const response = await fetch('/api/demo/award', { method }); const payload = await response.json(); if (!response.ok) throw new Error(payload.error); setAward(payload.award); } catch (reason) { setError(reason instanceof Error ? reason.message : 'Award action failed.'); } finally { setBusy(false); } } return <section className="mt-6 rounded-xl border border-[var(--line-strong)] bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow">Buyer decision</p><h2 className="mt-2 text-lg font-semibold">Award recommendation</h2><p className="mt-1 text-xs leading-5 text-[var(--muted)]">Create a deterministic split-award draft, review its caveats, then explicitly accept it before export.</p></div><div className="flex gap-2">{award?.status === 'DRAFT' && <button disabled={busy} onClick={() => void mutate('POST')} className="rounded-lg border border-[var(--line-strong)] px-4 py-2.5 text-xs font-semibold disabled:opacity-50">Recalculate</button>}{!award ? <button disabled={busy} onClick={() => void mutate('POST')} className="rounded-lg bg-[var(--ink)] px-4 py-2.5 text-xs font-semibold text-white disabled:opacity-50">{busy ? 'Creating…' : 'Create recommendation'}</button> : award.status === 'DRAFT' ? <button title={award.unawardedLines > 0 ? 'Recalculate after resolving the listed lines.' : undefined} disabled={busy || award.unawardedLines > 0} onClick={() => void mutate('PUT')} className="rounded-lg bg-[var(--ink)] px-4 py-2.5 text-xs font-semibold text-white disabled:opacity-50">{busy ? 'Working…' : 'Accept recommendation'}</button> : <a href="/api/demo/award/pdf" className="flex items-center gap-2 rounded-lg bg-[var(--ink)] px-4 py-2.5 text-xs font-semibold text-white"><Download size={13} /> Export PDF</a>}</div></div>{error && <p className="mt-3 text-xs text-[var(--red)]">{error}</p>}{award && <div className="mt-5 grid gap-4 border-t border-[var(--line)] pt-5 sm:grid-cols-3"><div><p className="eyebrow">Recommended spend</p><p className="mt-1 text-base font-semibold">{money.format(award.totalSpend)}</p></div><div><p className="eyebrow">Savings</p><p className="mt-1 text-base font-semibold">{award.savings === undefined ? 'No baseline' : money.format(award.savings)}</p></div><div><p className="eyebrow">Unawarded lines</p><p className="mt-1 text-base font-semibold">{award.unawardedLines}</p></div><div className="sm:col-span-3"><table className="w-full text-left text-[11px]"><thead><tr className="bg-[var(--surface)]"><th className="px-3 py-2">Vendor</th><th className="px-3 py-2">Lines</th><th className="px-3 py-2 text-right">Spend</th></tr></thead><tbody>{award.spendByVendor.slice(0, 5).map((item) => <tr key={item.vendor} className="border-t border-[var(--line)]"><td className="px-3 py-2">{item.vendor}</td><td className="px-3 py-2">{item.lines}</td><td className="px-3 py-2 text-right">{money.format(item.spend)}</td></tr>)}</tbody></table></div>{award.unawardedLines > 0 && <p className="text-xs text-amber-700 sm:col-span-3">Blocked by: {(award.unawardedLineIds?.length ? award.unawardedLineIds : [`${award.unawardedLines} lines in this older draft`]).join(', ')}. Recalculate after clarifications to refresh eligibility.</p>}</div>}</section>; }
